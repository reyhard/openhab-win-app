<Window
    x:Class="OpenHab.Windows.Tray.FlyoutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenHab.Windows.Tray"
    Title="openHAB">
    <Border x:Name="FlyoutChrome"
            Padding="5"
            Background="Transparent"
            BorderBrush="Transparent"
            BorderThickness="0">
        <Grid RowSpacing="8">
            <Grid.Resources>
                <MenuFlyout x:Name="SitemapMenuFlyout" />
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Image Grid.Column="0"
                       Width="20"
                       Height="20"
                       VerticalAlignment="Center">
                    <Image.Source>
                        <SvgImageSource UriSource="ms-appx:///Assets/openhab-icon.svg" />
                    </Image.Source>
                </Image>

                <StackPanel x:Name="SitemapHeaderArea"
                            Grid.Column="1"
                            Spacing="2"
                            Background="Transparent"
                            IsTapEnabled="True"
                            Tapped="SitemapHeaderArea_Tapped">
                    <TextBlock x:Name="TitleText"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Text="openHAB" />
                    <TextBlock x:Name="StatusText"
                               Opacity="0.72"
                               Style="{StaticResource CaptionTextBlockStyle}" />
                </StackPanel>

                <Button x:Name="SearchButton"
                        x:Uid="Flyout_SearchButton"
                        Grid.Column="2"
                        Width="34"
                        Height="34"
                        Padding="0"
                        ToolTipService.ToolTip="Search sitemap"
                        Click="SearchButton_Click">
                    <FontIcon x:Name="SearchButtonIcon"
                              Glyph="&#xE721;"
                              FontSize="13"
                              Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
                </Button>

                <Button x:Name="VoiceCommandButton"
                        Grid.Column="3"
                        Width="34"
                        Height="34"
                        Padding="0"
                        Visibility="Collapsed"
                        ToolTipService.ToolTip="Voice command"
                        Click="VoiceCommandButton_Click">
                    <FontIcon Glyph="&#xE720;" FontSize="13" />
                </Button>

                <Button x:Uid="Flyout_MinimizeButton"
                        Grid.Column="4"
                        Width="34"
                        Height="34"
                        Padding="0"
                        ToolTipService.ToolTip="Minimize flyout"
                        Click="MinimizeFlyoutButton_Click">
                    <FontIcon Glyph="&#xE70D;" FontSize="13" />
                </Button>
            </Grid>

            <Grid Grid.Row="1"
                  x:Name="NavigationSearchRow"
                  Margin="0,2,0,0">
                <BreadcrumbBar x:Name="BreadcrumbBar"
                               ItemClicked="BreadcrumbBar_ItemClicked">
                    <BreadcrumbBar.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Label}"
                                       FontFamily="{Binding FontFamily}"
                                       FontSize="{Binding FontSize}" />
                        </DataTemplate>
                    </BreadcrumbBar.ItemTemplate>
                </BreadcrumbBar>

                <AutoSuggestBox x:Name="SitemapSearchBox"
                                x:Uid="Flyout_SitemapSearchBox"
                                Visibility="Collapsed"
                                PlaceholderText="Search current section..."
                                QueryIcon="Find"
                                TextChanged="SitemapSearchBox_TextChanged"
                                QuerySubmitted="SitemapSearchBox_QuerySubmitted" />
            </Grid>

            <Grid x:Name="SitemapContentRoot"
                  Grid.Row="2"
                  Padding="8">
                <!-- Dual-slot layout for simultaneous page transitions.
                     Slot A is the active rendering surface; Slot B is the
                     off-screen peer populated before crossfade animation. -->
                <Grid x:Name="SitemapPageSlotA"
                      Background="{ThemeResource LayerFillColorAltBrush}">
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                  HorizontalScrollMode="Disabled"
                                  HorizontalAlignment="Stretch">
                        <StackPanel x:Name="SitemapRows" Spacing="4">
                            <StackPanel.Transitions>
                                <TransitionCollection>
                                    <RepositionThemeTransition />
                                    <AddDeleteThemeTransition />
                                </TransitionCollection>
                            </StackPanel.Transitions>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
                <Grid x:Name="SitemapPageSlotB"
                      Visibility="Collapsed"
                      Background="{ThemeResource LayerFillColorAltBrush}">
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                  HorizontalScrollMode="Disabled"
                                  HorizontalAlignment="Stretch">
                        <StackPanel x:Name="SitemapRowsB" Spacing="4">
                            <StackPanel.Transitions>
                                <TransitionCollection>
                                    <RepositionThemeTransition />
                                    <AddDeleteThemeTransition />
                                </TransitionCollection>
                            </StackPanel.Transitions>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Grid>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="4" HorizontalAlignment="Right">
                <Button x:Uid="Flyout_RefreshButton" Click="RefreshButton_Click" ToolTipService.ToolTip="Refresh" Width="34" Height="34" Padding="0">
                    <FontIcon Glyph="&#xE72C;" FontSize="13" />
                </Button>
                <Button x:Name="NotificationsButton"
                        x:Uid="Flyout_NotificationsButton"
                        Click="OpenNotificationsButton_Click"
                        ToolTipService.ToolTip="Open notifications"
                        Width="34"
                        Height="34"
                        Padding="0">
                    <Grid>
                        <FontIcon Glyph="&#xf2a3;" FontSize="13" />
                        <Border x:Name="UnreadBadge"
                                Background="{ThemeResource AccentFillColorDefaultBrush}"
                                CornerRadius="7"
                                MinWidth="14"
                                Height="14"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Top"
                                Visibility="Collapsed">
                            <TextBlock x:Name="UnreadBadgeText"
                                       FontSize="9"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       TextAlignment="Center"
                                       Margin="2,0,2,0" />
                        </Border>
                    </Grid>
                </Button>
                <Button x:Uid="Flyout_OpenMainWindowButton" Click="OpenAppButton_Click" ToolTipService.ToolTip="Open main window" Width="34" Height="34" Padding="0">
                    <FontIcon Glyph="&#xE8A7;" FontSize="13" />
                </Button>
                <Button x:Uid="Flyout_SettingsButton" Click="SettingsButton_Click" ToolTipService.ToolTip="Settings" Width="34" Height="34" Padding="0">
                    <FontIcon Glyph="&#xE713;" FontSize="13" />
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
