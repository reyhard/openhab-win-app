<Window
    x:Class="OpenHab.Windows.Tray.FlyoutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="openHAB">
    <Grid Margin="12" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="2">
            <TextBlock x:Name="TitleText"
                       FontSize="18"
                       FontWeight="SemiBold"
                       Text="openHAB"
                       IsTapEnabled="True"
                       Tapped="TitleText_Tapped">
                <FlyoutBase.AttachedFlyout>
                    <MenuFlyout x:Name="SitemapMenuFlyout" />
                </FlyoutBase.AttachedFlyout>
            </TextBlock>
            <TextBlock x:Name="StatusText"
                       Opacity="0.8" />
        </StackPanel>

        <ScrollViewer Grid.Row="1">
            <StackPanel x:Name="SitemapRows" Spacing="8" />
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="4" HorizontalAlignment="Right">
            <Button x:Name="BackButton" Click="NavigateBack_Click" ToolTipService.ToolTip="Back" Width="36" Height="36" Padding="4"
                    Visibility="Collapsed">
                <FontIcon Glyph="&#xE72B;" FontSize="14" />
            </Button>
            <Button Click="RefreshButton_Click" ToolTipService.ToolTip="Refresh" Width="36" Height="36" Padding="4">
                <FontIcon Glyph="&#xE72C;" FontSize="14" />
            </Button>
            <Button Click="OpenAppButton_Click" ToolTipService.ToolTip="Open main window" Width="36" Height="36" Padding="4">
                <FontIcon Glyph="&#xE8A1;" FontSize="14" />
            </Button>
            <Button Click="SettingsButton_Click" ToolTipService.ToolTip="Settings" Width="36" Height="36" Padding="4">
                <FontIcon Glyph="&#xE713;" FontSize="14" />
            </Button>
        </StackPanel>
    </Grid>
</Window>
