<Window
    x:Class="OpenHab.Windows.Tray.FlyoutWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenHab.Windows.Tray"
    Title="openHAB">
    <Border x:Name="FlyoutChrome"
            Margin="1"
            Padding="1"
            Background="{ThemeResource LayerFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="4"
            CornerRadius="14">
        <Grid RowSpacing="8">
            <Grid.Resources>
                <MenuFlyout x:Name="SitemapMenuFlyout" />
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid ColumnSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button x:Name="BackButton"
                        Width="34"
                        Height="34"
                        Padding="0"
                        Click="NavigateBack_Click"
                        ToolTipService.ToolTip="Back"
                        Visibility="Collapsed">
                    <FontIcon Glyph="&#xE72B;" FontSize="13" />
                </Button>

                <Image Grid.Column="1"
                       Width="20"
                       Height="20"
                       VerticalAlignment="Center">
                    <Image.Source>
                        <SvgImageSource UriSource="ms-appx:///Assets/openhab-icon.svg" />
                    </Image.Source>
                </Image>

                <StackPanel x:Name="SitemapHeaderArea"
                            Grid.Column="2"
                            Spacing="2"
                            Background="Transparent"
                            IsTapEnabled="True"
                            Tapped="SitemapHeaderArea_Tapped">
                    <TextBlock x:Name="TitleText"
                               FontSize="20"
                               FontWeight="SemiBold"
                               Text="openHAB" />
                    <TextBlock x:Name="StatusText"
                               Opacity="0.72"
                               Style="{StaticResource CaptionTextBlockStyle}" />
                </StackPanel>

                <Button Grid.Column="3"
                        Width="34"
                        Height="34"
                        Padding="0"
                        ToolTipService.ToolTip="Minimize flyout"
                        Click="MinimizeFlyoutButton_Click">
                    <FontIcon Glyph="&#xE70D;" FontSize="13" />
                </Button>
            </Grid>

            <BreadcrumbBar Grid.Row="1"
                           x:Name="BreadcrumbBar"
                           ItemClicked="BreadcrumbBar_ItemClicked"
                           Margin="0,2,0,0">
                <BreadcrumbBar.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Label}"
                                   FontFamily="{Binding FontFamily}"
                                   FontSize="{Binding FontSize}" />
                    </DataTemplate>
                </BreadcrumbBar.ItemTemplate>
            </BreadcrumbBar>

            <Border Grid.Row="2"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="8">
                <ScrollViewer>
                    <StackPanel x:Name="SitemapRows" Spacing="4" />
                </ScrollViewer>
            </Border>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="4" HorizontalAlignment="Right">
                <Button Click="RefreshButton_Click" ToolTipService.ToolTip="Refresh" Width="34" Height="34" Padding="0">
                    <FontIcon Glyph="&#xE72C;" FontSize="13" />
                </Button>
                <Button Click="OpenAppButton_Click" ToolTipService.ToolTip="Open main window" Width="34" Height="34" Padding="0">
                    <FontIcon Glyph="&#xE8A7;" FontSize="13" />
                </Button>
                <Button Click="SettingsButton_Click" ToolTipService.ToolTip="Settings" Width="34" Height="34" Padding="0">
                    <FontIcon Glyph="&#xE713;" FontSize="13" />
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
