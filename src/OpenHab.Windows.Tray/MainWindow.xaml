<Window
    x:Class="OpenHab.Windows.Tray.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mainui="using:OpenHab.Windows.Tray.MainUi"
    Title="openHAB">
    <Grid Margin="20" RowSpacing="12" ColumnSpacing="0">
        <Grid.Resources>
            <MenuFlyout x:Name="SitemapMenuFlyout" />
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0"
                Grid.RowSpan="3"
                Grid.Column="0"
                Margin="-20,-20,12,-20"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="0,0,1,0">
            <Grid Padding="12,20,12,12"
                  RowSpacing="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel Spacing="8">
                    <StackPanel Orientation="Horizontal"
                                Spacing="8"
                                Margin="4,0,0,14">
                        <Image Width="28"
                               Height="28">
                            <Image.Source>
                                <SvgImageSource UriSource="ms-appx:///Assets/openhab-icon.svg" />
                            </Image.Source>
                        </Image>
                        <StackPanel>
                            <TextBlock Text="openHAB"
                                       FontSize="18"
                                       FontWeight="SemiBold" />
                            <TextBlock Text="Windows companion"
                                       Opacity="0.6"
                                       Style="{StaticResource CaptionTextBlockStyle}" />
                        </StackPanel>
                    </StackPanel>
                    <Button x:Name="HomeNavButton"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Click="HomeNavButton_Click">
                        <StackPanel Orientation="Horizontal"
                                    Spacing="8">
                            <FontIcon Glyph="&#xE80F;"
                                      FontSize="14" />
                            <TextBlock Text="Home" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="MainUiPagesToggleButton"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Click="MainUiPagesToggleButton_Click">
                        <StackPanel Orientation="Horizontal"
                                    Spacing="8">
                            <FontIcon x:Name="MainUiPagesChevron"
                                      Glyph="&#xE70D;"
                                      FontSize="12" />
                            <TextBlock Text="Main UI Pages" />
                        </StackPanel>
                    </Button>
                    <StackPanel x:Name="MainUiPagesList"
                                Spacing="4"
                                Margin="18,0,0,0" />
                    <Button x:Name="NotificationsNavButton"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Click="NotificationsNavButton_Click">
                        <StackPanel Orientation="Horizontal"
                                    Spacing="8">
                            <FontIcon Glyph="&#xE7F4;"
                                      FontSize="14" />
                            <TextBlock Text="Notifications" />
                        </StackPanel>
                    </Button>
                    <Button x:Name="SettingsNavButton"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Click="SettingsNavButton_Click">
                        <StackPanel Orientation="Horizontal"
                                    Spacing="8">
                            <FontIcon Glyph="&#xE713;"
                                      FontSize="14" />
                            <TextBlock Text="Settings" />
                        </StackPanel>
                    </Button>
                </StackPanel>
                <StackPanel Grid.Row="2"
                            Spacing="2">
                    <TextBlock x:Name="ShellConnectionText"
                               Text="Connecting..."
                               Opacity="0.8" />
                    <TextBlock Text="to openHAB server"
                               Opacity="0.55"
                               Style="{StaticResource CaptionTextBlockStyle}" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="0"
              Grid.Column="1"
              ColumnSpacing="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button x:Name="BackButton"
                    Width="34"
                    Height="34"
                    Padding="0"
                    Click="NavigateBack_Click"
                    ToolTipService.ToolTip="Back"
                    Visibility="Collapsed"
                    VerticalAlignment="Top">
                <FontIcon Glyph="&#xE72B;" FontSize="13" />
            </Button>

            <Image Width="22"
                   Height="22"
                   Grid.Column="1"
                   VerticalAlignment="Top"
                   Margin="0,2,0,0">
                <Image.Source>
                    <SvgImageSource UriSource="ms-appx:///Assets/openhab-icon.svg" />
                </Image.Source>
            </Image>

            <StackPanel x:Name="SitemapHeaderArea"
                        Grid.Column="2"
                        Spacing="4"
                        Background="Transparent"
                        HorizontalAlignment="Left"
                        IsTapEnabled="True"
                        Tapped="SitemapHeaderArea_Tapped">
                <TextBlock x:Name="TitleText"
                           FontSize="24"
                           FontWeight="SemiBold" />
                <TextBlock x:Name="StatusText"
                           Opacity="0.6"
                           Style="{StaticResource CaptionTextBlockStyle}" />
            </StackPanel>

            <Button x:Name="ToggleSitemapButton"
                    Grid.Column="3"
                    VerticalAlignment="Center"
                    Click="ToggleSitemapButton_Click"
                    ToolTipService.ToolTip="Toggle sitemap pane"
                    Width="36"
                    Height="36"
                    Padding="4">
                <FontIcon Glyph="&#xF246;"
                          FontSize="14" />
            </Button>

            <Button Grid.Column="4"
                    VerticalAlignment="Center"
                    Click="RefreshButton_Click"
                    ToolTipService.ToolTip="Refresh"
                    Width="36"
                    Height="36"
                    Padding="4">
                <FontIcon Glyph="&#xE72C;"
                          FontSize="14" />
            </Button>
        </Grid>

        <Border Grid.Row="1"
                Grid.Column="1"
                Height="1"
                Margin="0,4,0,0"
                Background="{ThemeResource CardStrokeColorDefaultBrush}"
                Opacity="0.3" />

        <Grid Grid.Row="2"
              Grid.Column="1"
              ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition x:Name="SitemapPaneColumn"
                                  Width="0" />
            </Grid.ColumnDefinitions>

            <Grid x:Name="CenterContentHost"
                  Grid.Column="0">
                <mainui:MainUiWebViewHost x:Name="MainUiHost" />
            </Grid>

            <Border x:Name="SitemapContentRoot"
                    Grid.Column="1"
                    Visibility="Collapsed"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="12">
                <Grid>
                    <Grid x:Name="SitemapPageSlotA"
                          Background="Transparent">
                        <ScrollViewer>
                            <StackPanel x:Name="SitemapRows"
                                        Spacing="4" />
                        </ScrollViewer>
                    </Grid>
                    <Grid x:Name="SitemapPageSlotB"
                          Visibility="Collapsed"
                          Background="Transparent">
                        <ScrollViewer>
                            <StackPanel x:Name="SitemapRowsB"
                                        Spacing="4" />
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
