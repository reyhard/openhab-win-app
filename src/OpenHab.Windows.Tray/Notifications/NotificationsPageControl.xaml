<UserControl
    x:Class="OpenHab.Windows.Tray.Notifications.NotificationsPageControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12">
        <Grid RowSpacing="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="NotificationSearchBox" x:Uid="NotificationsPage_SearchBox" PlaceholderText="Search notifications" MinWidth="220" TextChanged="NotificationSearchBox_TextChanged" />
                <ComboBox Grid.Column="1" x:Name="NotificationFilterBox" MinWidth="120" Width="120" SelectedIndex="0" SelectionChanged="NotificationFilterBox_SelectionChanged">
                    <ComboBoxItem x:Uid="NotificationsPage_FilterVisible" Content="Visible" Tag="Visible" />
                    <ComboBoxItem x:Uid="NotificationsPage_FilterUnread" Content="Unread" Tag="Unread" />
                    <ComboBoxItem x:Uid="NotificationsPage_FilterRead" Content="Read" Tag="Read" />
                    <ComboBoxItem x:Uid="NotificationsPage_FilterHidden" Content="Hidden" Tag="Hidden" />
                    <ComboBoxItem x:Uid="NotificationsPage_FilterAll" Content="All" Tag="All" />
                </ComboBox>
                <ComboBox Grid.Column="2" x:Name="NotificationSortBox" MinWidth="110" Width="110" SelectedIndex="0" SelectionChanged="NotificationSortBox_SelectionChanged">
                    <ComboBoxItem x:Uid="NotificationsPage_SortNewest" Content="Newest" Tag="DateDescending" />
                    <ComboBoxItem x:Uid="NotificationsPage_SortOldest" Content="Oldest" Tag="DateAscending" />
                    <ComboBoxItem x:Uid="NotificationsPage_SortName" Content="Name" Tag="Name" />
                </ComboBox>
                <Border Grid.Column="3" x:Name="UnreadBadge" Background="{ThemeResource AccentFillColorDefaultBrush}" CornerRadius="10" Padding="6,2" HorizontalAlignment="Left" VerticalAlignment="Center" Visibility="Collapsed">
                    <TextBlock x:Name="UnreadCountText" FontSize="11" />
                </Border>
                <Button Grid.Column="4" x:Name="MarkAllReadButton" x:Uid="NotificationsPage_MarkAllReadButton" Content="Mark all read" Click="MarkAllReadButton_Click" />
            </Grid>
            <TextBlock x:Name="LocalOnlyNote" x:Uid="NotificationsPage_LocalOnlyNote" Grid.Row="1" Text="New notifications unavailable in Local-Only mode. Switch to Automatic or Cloud to receive alerts." Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" TextWrapping="Wrap" Visibility="Collapsed" Margin="0,8,0,0" />
            <Grid Grid.Row="2">
                <ListView x:Name="NotificationRowsList"
                          Margin="0,0,10,0"
                          SelectionMode="None"
                          IsItemClickEnabled="True"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ItemClick="NotificationRowsList_ItemClick"
                          ContainerContentChanging="NotificationRowsList_ContainerContentChanging">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsStackPanel Orientation="Vertical" CacheLength="0" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                    BorderThickness="1"
                                    CornerRadius="4">
                                <Grid Padding="8,9,10,9"
                                      ColumnSpacing="10"
                                      MinHeight="58">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Spacing="2">
                                        <TextBlock Text="{Binding Title}"
                                                   FontWeight="SemiBold"
                                                   TextWrapping="Wrap"
                                                   TextTrimming="CharacterEllipsis"
                                                   MaxLines="2" />
                                        <TextBlock Text="{Binding Message}"
                                                   Opacity="0.68"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   TextWrapping="Wrap"
                                                   TextTrimming="CharacterEllipsis"
                                                   MaxLines="3" />
                                        <Border Background="{ThemeResource CardStrokeColorDefaultBrush}"
                                                CornerRadius="4"
                                                Padding="6,1,6,1"
                                                HorizontalAlignment="Left"
                                                Visibility="{Binding SeverityVisibility}">
                                            <TextBlock Text="{Binding SeverityText}" FontSize="11" Opacity="0.6" />
                                        </Border>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1"
                                                Spacing="4"
                                                HorizontalAlignment="Right">
                                        <TextBlock Text="{Binding ElapsedText}"
                                                   Opacity="0.68"
                                                   Style="{StaticResource CaptionTextBlockStyle}" />
                                        <FontIcon Glyph="&#xEA3A;"
                                                  FontSize="8"
                                                  HorizontalAlignment="Right"
                                                  Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                                  Visibility="{Binding UnreadVisibility}" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0,0,0,6" />
                            <Setter Property="MinHeight" Value="0" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
                <TextBlock x:Name="EmptyNotificationsText" x:Uid="NotificationsPage_EmptyText" Text="No notifications" Opacity="0.5" HorizontalAlignment="Center" Margin="0,40,0,0" Visibility="Collapsed" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
